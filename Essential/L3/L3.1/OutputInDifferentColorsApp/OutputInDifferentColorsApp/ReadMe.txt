The task:
  
     Create the Printer class.
     In the body of the class, create a void Print (string value) method that displays the value of the argument.
     Realize the possibility that, in case of inheritance from this class (Printer) of other classes (PPrintCol) and calling the corresponding:
         - method of their instance;
         - lines passed as arguments to methods were displayed in different colors.
     Be sure to use type casting.